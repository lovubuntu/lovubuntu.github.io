<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Annotations | My Blog]]></title>
  <link href="http://lovubuntu.github.io/blog/categories/annotations/atom.xml" rel="self"/>
  <link href="http://lovubuntu.github.io/"/>
  <updated>2016-03-24T00:25:31+01:00</updated>
  <id>http://lovubuntu.github.io/</id>
  <author>
    <name><![CDATA[Prabu]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Cucumber Tests Running and Negating With Tags]]></title>
    <link href="http://lovubuntu.github.io/blog/2016/03/24/cucumber-tests-running-and-negating-with-tags/"/>
    <updated>2016-03-24T00:15:23+01:00</updated>
    <id>http://lovubuntu.github.io/blog/2016/03/24/cucumber-tests-running-and-negating-with-tags</id>
    <content type="html"><![CDATA[<p>Instead of running the entire test suite Cucumber gives us the option to run only a particular set of tests using <em>Tags</em>.<br/>
    Example:
        <code>cucumber --tags @unit</code></p>

<p>If more than one tag should be executed then a comma separated list of values will do the trick.
    Example:
        <code>cucumber --tags @unit,@integration</code><br/>
Here the comma separated annotations is considered as <code>Logical OR</code> condition</p>

<p>But I was tasked to <em>NOT</em> to run a set of tests which have the specified tag (Because those tests should run separately).<br/>
Cucumber is giving an option to do that as well. We can just use <code>cucumber --tags ~@donotrun</code></p>

<p>Wait what if I don&rsquo;t want to run tests from more than one tag. If think like:
<code>cucumber --tags ~donotrun,~neverrun</code> then you are wrong. As mentioned some lines before the comma between the tags represents logical OR.</p>

<p>So it becomes <code>cucumber --tags (~donotrun || ~neverrun)</code> which practically runs all the tests without negating anything.</p>

<blockquote><p>Solution:<br/>
If the comma is logical OR then what we need is logical And. That is achieved by calling the &ndash;tags option again.
    <code>cucumber --tags ~donotrun --tags ~neverrun</code></p></blockquote>
]]></content>
  </entry>
  
</feed>
